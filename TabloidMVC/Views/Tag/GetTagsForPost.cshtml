@model TabloidMVC.Models.ViewModels.PostTagsViewModel

@{
    ViewData["Title"] = "GetTagsForPost";
}

<h1>GetTagsForPost</h1>

<h4>Tag</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="GetTagsForPost">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">

                <input type="hidden" asp-for="post.Id" class="form-control" />

            </div>
            <div class="form-group">
                <label asp-for="tag.Id" class="control-label">Tags</label>
                <select asp-for="tag.Id" class="form-control">
                    <option value="">Select Tag</option>
                    @foreach (Tag tag in Model.tags)
                    {
                        <option value="@tag.Id">@tag.Name</option>
                    }
                </select>
                <span asp-validation-for="tag.Id" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
